To run the code, open a console within project root directory. You must use the command:
      "npm install --legacy-peer-deps"
Without the quotes. Then run:
      "npm start"
      
Also without the quotes.
The "--legacy-peer-deps" flag is required on the first command.

ALternatively copy and paste the below into console:

npm install --legacy-peer-deps
npm run
